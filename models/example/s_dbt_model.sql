
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
    
    
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
    
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
    
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
    
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
    
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
    
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
    
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
-- Use the `ref` function to select from other models

select *
from {{ ref('my_first_dbt_model') }}
where id = 1


SELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transactionSELECT
    month_date,
    total_transaction,
    SUM(total_revenue) AS cat_revenues
FROM
    {{ ref('monthly_aggregates') }},
    UNNEST(purchase_categories) AS pc
WHERE 
    pc = 'Shorts'
GROUP BY
    month_date,
    total_transaction
